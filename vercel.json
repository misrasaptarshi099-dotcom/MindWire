{
  "buildCommand": "npm run build --workspace=@mindwire/shared && npm run build --workspace=web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm ci",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
